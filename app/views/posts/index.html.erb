
<div class='user-posts-list-container'>    
     <%=render partial:'shared/user_card', locals:{user:@user}%>
     <section>
       <% @posts.each_with_index do|post, index| %>
        <a href='posts/<%=post.id%>' class='link-to-specific-post'>
            <%=render partial:'shared/post_card', locals:{post:post,index:index} %>
            <div class='post-comments-container'>
                <%post.comments_per_post.each do |comment| %>
                    <%=render partial:'shared/comment_card', locals:{comment:comment,user:@user, post:post} %> 
                <%end%>
            </div>           
        </a>        
        <%end%>
     </section>  
     <div class='pagination-container'>
        <%= will_paginate @posts, renderer: WillPaginate::ActionView::LinkRenderer, next_label: 'Next' %>
     </div>
    
</div>